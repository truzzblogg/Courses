# Let's create a multiplication table in Python
# Method 1:

import sys
def multi_table():
    num = int(input("Please, enter a number: "))
    limit = int(input("What is the limit of the multiplication table? "))

    for i in range(1, limit + 1):
        product = num * i
        print(f"{num} * {i} = {product}")

    print("Create another table? \n 1 - Yes \n 2 - No")
    x = input().lower()
    if (x == '1' or x == 'yes'):
        multi_table()
    else:
        print("Bye Bye!!!")
        sys.exit(1)

multi_table()

# Method 2: Using a simplified version of the 'if else' sentence
# Note: You have to remove the '#' symbol on each line in order to run the code

# Let's create a multiplication table in Python

# import sys
# def multi_table():
#     num = int(input("Please, enter a number: "))
#     limit = int(input("What is the limit of the multiplication table? "))
# 
#     for i in range(1, limit + 1):
#         product = num * i
#         print(f"{num} * {i} = {product}")
# 
#     print("Create another table? \n 1 - Yes \n 2 - No")
#     x = input().lower()
#     selec = (1, '1', 'yes', 'y')
#     if (x in selec):
#         multi_table()
#     else:
#         print("Bye Bye!!!")
#         sys.exit(1)
# 
# multi_table()
